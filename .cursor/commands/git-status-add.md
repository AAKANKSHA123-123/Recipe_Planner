/git-status-add check git status and show modified files

**Delegate:** Use the Task tool (mcp_task) with `subagent_type="git-status-add"` to launch this as a subagent. Pass the task in the prompt parameter. Do not run git commands directly.
